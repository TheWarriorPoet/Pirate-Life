using UnityEngine;
using System.Collections;

public class PirateMenuAnimator : MonoBehaviour {

	// Use this for initialization
	void Start () {
	GetComponent<Animator>().Play("drunk_idle");
            }
	
	// Update is called once per frame
	void Update () {
	
	}
}
